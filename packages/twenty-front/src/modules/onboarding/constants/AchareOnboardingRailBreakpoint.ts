/**
 * Below this width the onboarding rail is hidden and the step content takes the
 * full window. Kept in one place because both the rail and the shell's
 * mobile-only brand mark switch at the same breakpoint.
 */
export const ACHARE_ONBOARDING_RAIL_BREAKPOINT = 920;
