/**
 * Version of the Achare workspace feature-configuration schema.
 *
 * Bump when the shape of `AchareWorkspaceFeatureConfiguration` changes so that
 * existing workspaces can be migrated safely on next read.
 */
export const ACHARE_FEATURE_CONFIGURATION_VERSION = 1;
